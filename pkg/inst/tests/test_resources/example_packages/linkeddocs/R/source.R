#
# vim:set ff=unix expandtab ts=2 sw=2:
#------------------------------------------------
mmNameSpaceInfo<-function( # a fake only there to test if we can avoid overloading the original function in linkeddocs
pkgDir){
  return(5)
}

