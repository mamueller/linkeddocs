#
# vim:set ff=unix expandtab ts=2 sw=2:
fakePackageName<-function #make up a package name to associate with the environment when the code is sourced
### this function should also read the DESCRIPTION if there is one
### the faked name is necessary to build S4 generics and classes
(){ "inlinedocs.processor"}

